<?xml version="1.0" encoding="UTF-8"?>
<ViewCell xmlns="http://xamarin.com/schemas/2014/forms" 
          xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
          x:Class="CompucareWard.Controls.PickerCell"
          
          x:Name="InternalPickerCell">
    <ViewCell.View>
        <StackLayout Orientation="Horizontal" BindingContext="{x:Reference InternalPickerCell}" Padding="16,0">
            <Label Text="{Binding Label}" VerticalOptions="Center" />
            <Picker x:Name="InternalPicker" Title="{Binding Title}" VerticalOptions="Center" HorizontalOptions="FillAndExpand" SelectedIndexChanged="OnPickerSelectedIndexChanged" />
        </StackLayout>
    </ViewCell.View>
</ViewCell>